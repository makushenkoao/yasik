import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  imageBackground: {
    width: '100%',
    height: 500,
    borderRadius: 12,
    overflow: 'hidden',
  },
  container: {
    marginVertical: 20,
  },
  content: {
    height: '100%',
    justifyContent: 'space-between',
  },
});
