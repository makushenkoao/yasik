import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    marginTop: 140,
    marginBottom: 40,
  },
  content: {height: '100%', justifyContent: 'space-between'},
});
